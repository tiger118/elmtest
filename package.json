{
  "name": "decoder",
  "version": "1.0.0",
  "description": "`src/Decoder.elm` is meant to be a module that is capable of \"decoding\" a data structure of `List ( k, String )`",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm-test": "^0.19.1-revision7"
  }
}
